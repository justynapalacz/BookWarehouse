package palaczjustyna.bookWarehouse.entity;

public record EmployeeDTO(
        Integer id,
        String firstName,
        String lastName,
        String phone,
        String email

        ) {
}
